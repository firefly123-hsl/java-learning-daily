import java.time.LocalDateTime;import java.util.HashMap;import java.util.List;import java.util.Map;public class Session {    private String showtimeId;    private String movieId;    private String hallId;    private LocalDateTime startTime;    // 新增：场次调价系数（如黄金场1.1，早场0.9，默认1.0）    private double priceCoefficient = 1.0;    private Map<String, SeatStatus> seatStatusMap;    // 构造器：可指定场次系数    public Showtime(String showtimeId, String movieId, String hallId, LocalDateTime startTime, double priceCoefficient, CinemaSystemManager manager) {        this.showtimeId = showtimeId;        this.movieId = movieId;        this.hallId = hallId;        this.startTime = startTime;        this.priceCoefficient = priceCoefficient; // 场次系数        // 初始化座位状态（原有逻辑）        CinemaHall hall = manager.getHallById(hallId);        this.seatStatusMap = new HashMap<>();        if (hall != null) {            for (entity.Seat.Seat seat : hall.getSeatList()) {                seatStatusMap.put(seat.getSeatId(), SeatStatus.AVAILABLE);            }        }    }    // ======== 核心：座位锁定/解锁（基础方法） ========    // 锁定座位（关联订单）    public boolean lockSeats(List<entity.Seat.Seat> seats, String orderId) {        // 校验：所有座位必须可选，且属于本场次影厅        for (entity.Seat.Seat seat : seats) {            if (!seatStatusMap.containsKey(seat.getSeatId())                    || seatStatusMap.get(seat.getSeatId()) != SeatStatus.AVAILABLE) {                return false; // 有座位不可锁定，整体失败            }        }        // 锁定所有座位        for (entity.Seat.Seat seat : seats) {            seatStatusMap.put(seat.getSeatId(), SeatStatus.LOCKED);            seat.setLockOrderId(orderId); // 关联订单ID        }        return true;    }    // 解锁座位（取消锁定/超时解锁）    public boolean unlockSeats(List<entity.Seat.Seat> seats) {        // 校验：所有座位必须是锁定状态        for (entity.Seat.Seat seat : seats) {            if (!seatStatusMap.containsKey(seat.getSeatId())                    || seatStatusMap.get(seat.getSeatId()) != SeatStatus.LOCKED) {                return false; // 有座位不是锁定状态，整体失败            }        }        // 解锁所有座位        for (entity.Seat.Seat seat : seats) {            seatStatusMap.put(seat.getSeatId(), SeatStatus.AVAILABLE);            seat.setLockOrderId(null); // 清空订单关联        }        return true;    }    // 支付成功：锁定→已售    public boolean confirmSeats(List<entity.Seat.Seat> seats) {        for (entity.Seat.Seat seat : seats) {            if (!seatStatusMap.containsKey(seat.getSeatId())                    || seatStatusMap.get(seat.getSeatId()) != SeatStatus.LOCKED) {                return false;            }        }        for (entity.Seat.Seat seat : seats) {            seatStatusMap.put(seat.getSeatId(), SeatStatus.SOLD);            seat.setLockOrderId(null); // 清空订单关联        }        return true;    }    // Getter & Setter    public String getShowtimeId() { return showtimeId; }    public void setShowtimeId(String showtimeId) { this.showtimeId = showtimeId; }    public String getMovieId() { return movieId; }    public void setMovieId(String movieId) { this.movieId = movieId; }    public String getHallId() { return hallId; }    public void setHallId(String hallId) { this.hallId = hallId; }    public LocalDateTime getStartTime() { return startTime; }    public void setStartTime(LocalDateTime startTime) { this.startTime = startTime; }    public double getTicketPrice() { return ticketPrice; }    public void setTicketPrice(double ticketPrice) { this.ticketPrice = ticketPrice; }    public Map<String, SeatStatus> getSeatStatusMap() { return seatStatusMap; }    public void setSeatStatusMap(Map<String, SeatStatus> seatStatusMap) { this.seatStatusMap = seatStatusMap; }    public double getPriceCoefficient() { return priceCoefficient; }    public void setPriceCoefficient(double priceCoefficient) { this.priceCoefficient = priceCoefficient; }}