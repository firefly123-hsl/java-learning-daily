package strategy;import strategy.PriceCalculateStrategy;/** * 普通策略（无溢价/折扣，系数1.0） */class NormalPriceStrategy implements PriceCalculateStrategy {    @Override    public double calculate(double basePrice, double showtimeCoefficient) {        return basePrice * showtimeCoefficient;    }}/** * 节假日策略（统一抬高1.2倍） */class HolidayPriceStrategy implements PriceCalculateStrategy {    @Override    public double calculate(double basePrice, double showtimeCoefficient) {        return basePrice * showtimeCoefficient * 1.2; // 节假日溢价20%    }}/** * 折扣策略（统一8折） */class DiscountPriceStrategy implements PriceCalculateStrategy {    @Override    public double calculate(double basePrice, double showtimeCoefficient) {        return basePrice * showtimeCoefficient * 0.8; // 8折    }}